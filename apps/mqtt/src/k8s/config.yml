---
apiVersion: v1
kind: Secret
metadata:
  name: mosquitto-config
  namespace: mqtt
stringData:
  mosquitto.conf: |
    per_listener_settings false

    log_dest stdout
    log_type all

    allow_anonymous true

    persistence false

    listener 1883
    protocol mqtt
